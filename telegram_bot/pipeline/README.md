> **Not to be confused with [`../pipelines/`](../pipelines/)** (plural): `pipelines/` holds the
> client-direct entrypoints. This `pipeline/` (singular) is the
> supervisor-driven query path lifted out of the `bot.py` god-object.