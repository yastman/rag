"""Langfuse model sync stubs — Langfuse removed (#2844)."""

from __future__ import annotations

from typing import Any


def sync_langfuse_model_definitions(client: Any = None) -> int:
    """No-op stub — Langfuse removed (#2844)."""
    return 0
