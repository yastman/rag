"""Model sync stubs — tracing removed (#2844)."""

from __future__ import annotations

from typing import Any


def sync_model_definitions(client: Any = None) -> int:
    """No-op stub — tracing removed (#2844)."""
    return 0
