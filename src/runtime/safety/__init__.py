"""Transport-neutral safety helpers — prompt injection detection.

Canonical home of ``detect_injection`` / ``guard_node``; moved out of the
graph namespace by #3207 so live safety helpers no longer live under
``src/runtime/graph``.
"""
