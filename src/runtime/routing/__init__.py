"""Transport-neutral query routing — regex classification of user queries.

Canonical home of ``classify_query``; moved out of the graph namespace
by #3207 so live routing helpers no longer live under ``src/runtime/graph``.
"""
