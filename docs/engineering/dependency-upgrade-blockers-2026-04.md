# Dependency Upgrade Blockers

- `langfuse`: held at `3.14.6` instead of `4.x` because the current codebase still uses v3-only APIs such as `update_current_trace`, `start_as_current_span`, and `LangchainCallbackHandler(update_trace=...)`. Next step: migrate tracing/observation calls in `src/evaluation/langfuse_integration.py`, `telegram_bot/observability.py`, `src/voice/observability.py`, `src/ingestion/unified/observability.py`, and `src/api/main.py`, then re-run the bot/LangGraph wave.
- Docker/runtime image wave: no open Renovate image backlog was available to port onto `dev`, so the wave was a content no-op. Required validation remains partially blocked because this WSL session cannot reach a Docker daemon (`dockerDesktopLinuxEngine` pipe unavailable), which prevents `make verify-compose-images` from completing. Next step: restore Docker Desktop WSL integration or run the final Compose/image drift checks from an environment with a working daemon, then complete Task 8 and final verification.
