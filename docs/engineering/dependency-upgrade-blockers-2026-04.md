# Dependency Upgrade Blockers

- `langfuse`: blocker resolved by the native v4 migration. The codebase now targets `langfuse>=4.0.0,<5.0` and uses the SDK's observation-first APIs directly across runtime paths and maintenance scripts.
