# Mini App archived

The Mini App implementation has moved to [`../archive/mini_app`](../archive/mini_app).

This directory is intentionally kept as a documentation-only compatibility shim
for legacy CI workflow path filters until those protected workflow files can be
updated with a token that has the GitHub `workflow` scope. Do not add runtime,
source, tests, or required-path assets here.
