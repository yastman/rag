"""CRM quick actions FSM migration contract — ARCHIVED (#2625).

The CRM surface (crm_callbacks.py, crm_quick_actions.py, crm_tools.py,
kommo_client, etc.) was archived in #2625 as part of the monolith cleanup
epic (#2596). The original contract pinned the FSM-to-dialog migration
from issue #2053.

This file is kept as a tombstone so git history shows intent. All test
functions have been removed because the referenced files no longer exist.
"""
