apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-base-model-cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
